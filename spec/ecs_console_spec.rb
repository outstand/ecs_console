require "spec_helper"

describe EcsConsole do
  it "has a version number" do
    expect(EcsConsole::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
